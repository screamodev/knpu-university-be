# Cached HTML of the old site — regenerated by the extractors, 14 MB, no value in git.
.cache/
__pycache__/

# source URL → uploaded file id. Per environment: the ids on the local Directus are not the ids
# on production, so committing it would make a prod run re-use identifiers that do not exist there.
files.map.json
