# Правки 25.09.2026 (п. 2, 3)

Google Doc клієнта «Правки 25.09.26», п. 2 (кафедра спеціальної педагогіки) і п. 3 (ННІ
спеціальної освіти і інклюзії). Матеріали — Word-файли з пошти й публічна тека Google Drive
кафедри спеціальної педагогіки.

| Що | Куди |
| --- | --- |
| 8 фото занять кафедри спеціальної педагогіки (Drive, тека «Zaняття») | медіатека; нова вкладка `university-departments` |
| 8 фото складу студентської ради ННІ спеціальної освіти (з `Студентська_рада_склад_оновлення.docx`) | медіатека; вкладка `students` → «Студентське життя» |
| вкладки `student-circle`, `university-departments` | `structure_pages.tab` (`apply_schema.py`) |

Увесь текст — у статичних JSON фронту (`app/content/structure/kafedra-specialnoyi-pedagogiky/`,
`app/content/structure/special-education/students.uk.json`); тут лише файли.

```bash
cd migration
python3 schema/apply_schema.py
python3 fixes-2026-09-25/push_assets.py --dry-run   # 16 файлів
python3 fixes-2026-09-25/push_assets.py
```

Фото стиснуті до 1600 px, JPEG q78 (через `python:3.12-slim` + Pillow — на цій машині немає
`sips`), лежать у `files/` в git.

## Чого тут немає і чому

* **Кафедра спеціальної педагогіки, п. 2, пп. 3–9 (докладний зміст вкладок «Наукова діяльність»,
  «Студентський науковий гурток», «Університетські кафедри»)** — весь текст узятий дослівно з
  `Розділ _Наукова діяльність_.docx`, `Гурток.docx`, `Університетські кафедри.docx`; окремих файлів
  не лишилось, усе увійшло у фронт.
* **Покликання на файли Google Drive** (плани роботи гуртка, положення про університетські
  кафедри тощо) лишаються на Google Drive — це чужі документи, не наші файли.
* **ННІ спеціальної освіти, п. 3** — новий склад студентської ради вставлено повністю зі
  `Студентська_рада_склад_оновлення.docx`, старий склад і новини 2021–2024 видалено за прямою
  вказівкою клієнта («видаляємо», «І всі наступні теж видаляємо»).
