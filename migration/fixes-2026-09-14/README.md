# Правки 14.09.2026

Два набори:

1. **Опис конференції у WYSIWYG** — `rich_conference_descriptions.py` (уже на проді).
2. **Центр якості освіти** — docx «Опис якість» і три PDF.

## Опис конференції

```bash
cd migration
python3 fixes-2026-09-14/rich_conference_descriptions.py --dry-run
python3 fixes-2026-09-14/rich_conference_descriptions.py
```

Скрипт перемикає інтерфейс полів `description` і `descriptionEn` і переводить наявні описи з
простого тексту в HTML (рядки → `<br>`, голі адреси → посилання). Уже HTML-описи не чіпає;
повторний запуск нічого не міняє.

## Центр якості освіти

| Що | Куди |
| --- | --- |
| 3 PDF результатів анкет 2026 за ОП «Тифлопедагогіка. Сурдопедагогіка» | медіатека; `quality-centre-quality.uk.json`, «Результати анкетування» ННІ спеціальної освіти та інклюзії |
| анкети «за освітніми програмами» по факультетах (зі старого smc.hnpu.edu.ua) | фронт, та сама сторінка, «Анкети» |
| нові форми онлайн-анкетування ДВВ магістратури | фронт, `quality-centre-students.uk.json` |
| підзаголовок «Розклад занять 2-го року навчання» | `documents.group` розділу `free-choice-master-schedule` |

```bash
cd migration
python3 fixes-2026-09-14/push_assets.py --dry-run   # 3 файли
python3 fixes-2026-09-14/push_assets.py

python3 fixes-2026-09-14/group_master_schedule.py --dry-run
python3 fixes-2026-09-14/group_master_schedule.py
```

PDF надіслав центр, байти лежать у `files/`.

## Чого тут немає і чому

* **Розклад занять 1-го року навчання (ДВВ магістратури)** — файлів не надіслали. Коли будуть,
  редактори додають їх у Documents, розділ «Вільний вибір — розклад занять (магістр)», з
  підзаголовком «Розклад занять 1-го року навчання».
